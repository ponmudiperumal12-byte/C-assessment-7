#include <stdio.h>

void disp_sum_alldigits()
{
    int sum = 0,n;
    printf ("enter the number:");
    scanf("%d", &n);

    while(n > 0)
    {
        sum = sum + (n % 10);
        n = n / 10;
    }

    printf("%d", sum);
}

int main() 
{
    disp_sum_alldigits();
}
