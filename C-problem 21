#include <stdio.h>
void disp_prime_count()
{
    int i = 2, count = 0;

    while(i <= 9)
    {
        if(i == 2 || i == 3 || i == 5 || i == 7)
            count++;

        i++;
    }

    printf("%d", count);
}

int main()
{
    disp_prime_count();
}
