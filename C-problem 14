#include <stdio.h>

void disp_reverse_number()
{
    int rev = 0;
    int n;
    printf ("enter the number:");
    scanf("%d", &n);
    while(n > 0)
    {
        rev = rev * 10 + (n % 10);
        n = n / 10;
    }

    printf("%d", rev);
}

int main()
{

    disp_reverse_number();
}
