#include <stdio.h>

void count_total_digits()
{
    int count = 0,n;
    printf("enter the number:");
    scanf("%d", &n);
    while(n > 0)
    {
        count++;
        n = n / 10;
    }

    printf("%d", count);
}

int main()
{
   
    count_total_digits() ;
}
